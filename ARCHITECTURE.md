# System Architecture

## High-Level Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    Kubernetes Quest                         │
│                Interactive Presentation System              │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│                  │         │                  │         │                  │
│  Mobile Voters   │◄────────│   Go Backend     │────────►│    Presenter     │
│  (Alpine.js)     │  WS     │   (WebSocket)    │   WS    │   (Alpine.js)    │
│                  │         │                  │         │                  │
└──────────────────┘         └──────────────────┘         └──────────────────┘
       │                             │                            │
       │                             │                            │
       ▼                             ▼                            ▼
   Vote on                    Aggregate &                    Display &
   Choices                    Broadcast                       Control
```

## Data Flow

### 1. Voting Flow
```
User Opens Voter                Server Registers              Vote Recorded
      │                               │                             │
      ├──── WebSocket Connect ────────┤                             │
      │                               │                             │
      ├──── Submit Vote ──────────────┼──── Aggregate ──────────────┤
      │                               │                             │
      │◄─── Real-time Results ────────┼──── Broadcast ◄─────────────┤
      │                               │                             │
```

### 2. Presentation Flow
```
Presenter                      Backend                         Content
    │                             │                               │
    ├─── Load Chapter ────────────┼─── Parse Markdown ────────────┤
    │                             │                               │
    │◄─── Return HTML ────────────┤                               │
    │                             │                               │
    ├─── Start Vote ──────────────┼─── Initialize Timer ──────────┤
    │                             │                               │
    │◄─── Vote Updates ───────────┼─── Broadcast Results ─────────┤
    │                             │                               │
    ├─── Advance Story ───────────┼─── Follow Choice Path ────────┤
    │                             │                               │
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Backend (Go)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │   Parser     │  │    Server    │  │  VoteManager │           │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤           │
│  │ Markdown     │  │ HTTP Routes  │  │ Vote State   │           │
│  │ Frontmatter  │  │ WebSocket    │  │ Aggregation  │           │
│  │ Story Flow   │  │ Static Files │  │ Broadcasting │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Content (Markdown)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  chapters/                           story.yaml                 │
│  ├── 01-intro.md                    ┌──────────────┐            │
│  ├── 02-choice.md                   │ flow:        │            │
│  ├── 03a-path-a.md                  │   start:     │            │
│  └── 03b-path-b.md                  │ nodes:       │            │
│                                     └──────────────┘            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Frontend (Alpine.js)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐              ┌──────────────────┐         │
│  │  Voter UI        │              │  Presenter UI    │         │
│  ├──────────────────┤              ├──────────────────┤         │
│  │ Vote Buttons     │              │ Chapter Display  │         │
│  │ Progress Bar     │              │ Vote Controls    │         │
│  │ Results Display  │              │ Live Results     │         │
│  │ User ID Cookie   │              │ Navigation       │         │
│  └──────────────────┘              └──────────────────┘         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## WebSocket Messages

### Client → Server
```json
{
  "type": "vote",
  "voter_id": "voter_abc123",
  "choice_id": "install-kcp"
}
```

### Server → Client
```json
{
  "type": "voting_started",
  "payload": {
    "question_id": "first-choice",
    "choices": ["install-kcp", "use-minikube"],
    "duration": 60
  }
}

{
  "type": "vote_update",
  "payload": {
    "question_id": "first-choice",
    "results": {
      "install-kcp": 15,
      "use-minikube": 8
    },
    "total": 23
  }
}

{
  "type": "voting_ended",
  "payload": {
    "question_id": "first-choice",
    "results": {
      "install-kcp": 42,
      "use-minikube": 28
    },
    "winner": "install-kcp"
  }
}

{
  "type": "chapter_changed",
  "payload": {
    "id": "install-kcp",
    "metadata": { ... },
    "content": "<h1>Chapter 2</h1>..."
  }
}
```

## Chapter Structure

```yaml
---
id: chapter-id
type: decision
timer: 60
choices:
  - id: choice-a
    label: "Option A"
    description: "Description"
    icon: "🚀"
    next: chapter-a
    risk: high
  - id: choice-b
    label: "Option B"
    description: "Description"
    icon: "🛡️"
    next: chapter-b
    risk: low
---

# Chapter Title

Your markdown content here...
```

## File System Layout

```
voting/
├── backend/
│   ├── main.go                 # Entry point
│   ├── parser/
│   │   ├── markdown.go         # Parse MD + frontmatter
│   │   └── story.go           # Story flow engine
│   └── server/
│       ├── server.go          # HTTP + WebSocket server
│       └── votes.go           # Vote aggregation
├── frontend/
│   ├── index.html             # Landing page
│   ├── voter/
│   │   └── index.html         # Voting interface
│   └── presenter/
│       └── index.html         # Presentation view
├── content/
│   ├── chapters/              # Markdown chapters
│   │   ├── 01-intro.md
│   │   ├── 02-choice.md
│   │   └── ...
│   └── story.yaml            # Flow definition
├── Dockerfile
├── docker-compose.yml
└── README.md
```

## Decision Tree Example

```
         ┌─────────┐
         │  Start  │
         └────┬────┘
              │
              ▼
         ┌─────────┐
         │ Intro   │
         └────┬────┘
              │
              ▼
        ┌─────────────┐
        │ First Choice│
        │   (VOTE)    │
        └──────┬──────┘
               │
       ┌───────┼───────┐
       │       │       │
       ▼       ▼       ▼
   ┌─────┐ ┌─────┐ ┌──────┐
   │ KCP │ │Mini-│ │Coffee│
   │Path │ │kube │ │DEAD! │
   └──┬──┘ └──┬──┘ └──────┘
      │       │
      │       │
      ▼       ▼
   (Continue story...)
```

## State Management

```
┌──────────────────────────────┐
│      VoteManager State       │
├──────────────────────────────┤
│                              │
│ currentQuestion: "choice-1"  │
│ votes: {                     │
│   "choice-1": {              │
│     "option-a": 15,          │
│     "option-b": 23           │
│   }                          │
│ }                            │
│ voters: {                    │
│   "voter_123": "option-a",   │
│   "voter_456": "option-b"    │
│ }                            │
│ clients: [ws1, ws2, ...]     │
│ votingActive: true           │
│                              │
└──────────────────────────────┘
```

## Deployment Architecture (Production)

```
Internet
    │
    ▼
┌────────────┐
│   Nginx    │  Port 80/443
│  (Reverse  │  (SSL/TLS)
│   Proxy)   │
└─────┬──────┘
      │
      ▼
┌────────────┐
│   Docker   │
│ Container  │  Port 8080
│   (Go App) │
└────────────┘
      │
      ▼
┌────────────┐
│   Volume   │
│  /content  │  (Chapters)
└────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────┐
│              Frontend                   │
├─────────────────────────────────────────┤
│ Alpine.js 3.x                           │
│ Tailwind CSS 3.x                        │
│ Native WebSocket API                    │
│ LocalStorage (voter IDs)                │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│              Backend                    │
├─────────────────────────────────────────┤
│ Go 1.25+                                │
│ gorilla/mux (HTTP routing)              │
│ gorilla/websocket (WebSocket)           │
│ goldmark (Markdown parsing)             │
│ yaml.v3 (YAML parsing)                  │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│            Infrastructure               │
├─────────────────────────────────────────┤
│ Docker + Docker Compose                 │
│ Nginx (reverse proxy)                   │
│ Let's Encrypt (SSL)                     │
│ Linux (Ubuntu/Debian)                   │
└─────────────────────────────────────────┘
```

---

This architecture provides:
- ✅ Real-time communication
- ✅ Scalable to hundreds of voters
- ✅ Easy content authoring
- ✅ Simple deployment
- ✅ Mobile-friendly
- ✅ Professional presentation
